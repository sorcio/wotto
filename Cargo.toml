[workspace]
members = [
    "rustico",
    "wotto-irc",
    "rustico-cli",
    "wotto-utils",
]

[profile.release]
lto = "fat"
opt-level = "s"
strip = "debuginfo"
codegen-units = 1
overflow-checks = true

[patch.crates-io]
irc = { path = "irc" }
